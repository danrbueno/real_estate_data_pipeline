# name: "Feature branches: pytest and auto PR"

# on:
#   push:
#     # trigger on any branch that starts with "feature/"
#     branches:
#       - 'feature/**'

# permissions:
#   contents: write
#   pull-requests: write

# jobs:
#   test:
#     name: Run pytest
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.x'

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
#           pip install pytest

#       - name: Run pytest
#         id: pytest
#         run: |
#           pytest tests

#   create_pr:
#     name: Create Pull Request to 'main'
#     needs: test
#     if: needs.test.result == 'success'
#     runs-on: ubuntu-latest
#     steps:
#       - name: Create PR (if one doesn't already exist)
#         uses: actions/github-script@v6
#         with:
#           script: |
#             const owner = context.repo.owner;
#             const repo = context.repo.repo;
#             const head = process.env.GITHUB_REF.replace('refs/heads/','');
#             const base = 'main';
#             // check for existing open PR from this branch into base
#             const pulls = await github.rest.pulls.list({ owner, repo, head: `${owner}:${head}`, base, state: 'open' });
#             if (pulls.data.length > 0) {
#               console.log(`Open PR already exists: ${pulls.data[0].html_url}`);
#             } else {
#               const pr = await github.rest.pulls.create({
#                 owner,
#                 repo,
#                 title: `Auto: ${head} -> ${base}`,
#                 head,
#                 base,
#                 body: `This PR was created automatically after tests passed on branch ${head}.`
#               });
#               console.log(`Created PR: ${pr.data.html_url}`);
#             }

#   notify_failure:
#     name: Notify test failure
#     needs: test
#     if: needs.test.result == 'failure'
#     runs-on: ubuntu-latest
#     steps:
#       - name: Print failure message
#         run: |
#           echo "Tests failed on branch ${GITHUB_REF}. No PR created."
